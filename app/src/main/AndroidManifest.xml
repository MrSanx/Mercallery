<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.heklai.mercallery_pro">

    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MercalleryPro">
        <activity android:name=".EliminarPersona"></activity>
        <activity android:name=".EditarPersona" />
        <activity android:name=".DetallesPersona" />
        <activity android:name=".VerPersonas" />
        <activity android:name=".InsertarObra" />
        <activity android:name=".ActivityCrear" />
        <activity android:name=".SeleccionActividad" />
        <activity android:name=".InsertarPersona" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>